// sprites-batch5.js -- Auto-generated sprite variants
// Format: drawSprite_<name>_<variant>(ctx, cx, cy)

// Variant A: Classic gym bro in a tank top with big arms, holding a dumbbell
function drawSprite_fit_bro_A(ctx, cx, cy) {
  // Legs (dark shorts)
  ctx.fillStyle = '#1a1a2e';
  ctx.fillRect(cx - 5, cy - 12, 4, 12);
  ctx.fillRect(cx + 1, cy - 12, 4, 12);
  // Shoes
  ctx.fillStyle = '#e63946';
  ctx.fillRect(cx - 6, cy - 2, 5, 2);
  ctx.fillRect(cx + 1, cy - 2, 5, 2);
  // Shorts
  ctx.fillStyle = '#2d2d44';
  ctx.fillRect(cx - 6, cy - 18, 12, 6);
  // Torso (tank top - wide build)
  ctx.fillStyle = '#e63946';
  ctx.fillRect(cx - 8, cy - 30, 16, 12);
  // Skin showing at tank top sides
  ctx.fillStyle = '#d4a574';
  ctx.fillRect(cx - 9, cy - 28, 2, 8);
  ctx.fillRect(cx + 7, cy - 28, 2, 8);
  // Big arms
  ctx.fillStyle = '#d4a574';
  ctx.fillRect(cx - 12, cy - 30, 3, 12);
  ctx.fillRect(cx + 9, cy - 30, 3, 12);
  // Right hand holding dumbbell
  ctx.fillStyle = '#555555';
  ctx.fillRect(cx + 9, cy - 32, 3, 2);
  ctx.fillRect(cx + 7, cy - 33, 2, 4);
  ctx.fillRect(cx + 12, cy - 33, 2, 4);
  // Neck
  ctx.fillStyle = '#d4a574';
  ctx.fillRect(cx - 2, cy - 33, 4, 3);
  // Head
  ctx.fillStyle = '#d4a574';
  ctx.fillRect(cx - 5, cy - 40, 10, 7);
  // Hair (short buzz)
  ctx.fillStyle = '#3d2b1f';
  ctx.fillRect(cx - 5, cy - 40, 10, 2);
  // Sunglasses
  ctx.fillStyle = '#111111';
  ctx.fillRect(cx - 4, cy - 37, 3, 2);
  ctx.fillRect(cx + 1, cy - 37, 3, 2);
  // Mouth (grin)
  ctx.fillStyle = '#ffffff';
  ctx.fillRect(cx - 2, cy - 34, 4, 1);
}

// Variant B: Shirtless bro flexing both arms up, wearing a backwards cap
function drawSprite_fit_bro_B(ctx, cx, cy) {
  // Legs
  ctx.fillStyle = '#d4a574';
  ctx.fillRect(cx - 5, cy - 10, 4, 10);
  ctx.fillRect(cx + 1, cy - 10, 4, 10);
  // Shoes
  ctx.fillStyle = '#ffffff';
  ctx.fillRect(cx - 6, cy - 2, 5, 2);
  ctx.fillRect(cx + 1, cy - 2, 5, 2);
  // Gym shorts
  ctx.fillStyle = '#1e90ff';
  ctx.fillRect(cx - 7, cy - 17, 14, 7);
  // Torso (shirtless, tanned)
  ctx.fillStyle = '#c9956b';
  ctx.fillRect(cx - 8, cy - 29, 16, 12);
  // Abs detail
  ctx.fillStyle = '#b8845e';
  ctx.fillRect(cx - 2, cy - 27, 1, 8);
  ctx.fillRect(cx + 1, cy - 27, 1, 8);
  ctx.fillRect(cx - 4, cy - 25, 8, 1);
  ctx.fillRect(cx - 4, cy - 22, 8, 1);
  // Arms flexing UP (bicep pose)
  ctx.fillStyle = '#c9956b';
  ctx.fillRect(cx - 12, cy - 29, 4, 4);
  ctx.fillRect(cx - 14, cy - 36, 4, 7);
  ctx.fillRect(cx + 8, cy - 29, 4, 4);
  ctx.fillRect(cx + 10, cy - 36, 4, 7);
  // Bicep bulge
  ctx.fillStyle = '#d4a574';
  ctx.fillRect(cx - 15, cy - 34, 2, 3);
  ctx.fillRect(cx + 13, cy - 34, 2, 3);
  // Neck
  ctx.fillStyle = '#c9956b';
  ctx.fillRect(cx - 2, cy - 32, 4, 3);
  // Head
  ctx.fillStyle = '#c9956b';
  ctx.fillRect(cx - 5, cy - 39, 10, 7);
  // Backwards cap
  ctx.fillStyle = '#e63946';
  ctx.fillRect(cx - 5, cy - 40, 10, 2);
  ctx.fillRect(cx - 7, cy - 39, 2, 3);
  // Eyes (determined)
  ctx.fillStyle = '#111111';
  ctx.fillRect(cx - 3, cy - 37, 2, 2);
  ctx.fillRect(cx + 1, cy - 37, 2, 2);
  // Mouth (yelling motivation)
  ctx.fillStyle = '#8b4513';
  ctx.fillRect(cx - 2, cy - 34, 4, 2);
}

// Variant C: Bro in a hoodie with protein shaker, headband, more casual stance
function drawSprite_fit_bro_C(ctx, cx, cy) {
  // Legs (joggers)
  ctx.fillStyle = '#333344';
  ctx.fillRect(cx - 5, cy - 14, 4, 14);
  ctx.fillRect(cx + 1, cy - 14, 4, 14);
  // Shoes
  ctx.fillStyle = '#ff6b35';
  ctx.fillRect(cx - 6, cy - 2, 5, 2);
  ctx.fillRect(cx + 1, cy - 2, 5, 2);
  // Hoodie body
  ctx.fillStyle = '#444466';
  ctx.fillRect(cx - 9, cy - 29, 18, 15);
  // Hoodie pocket
  ctx.fillStyle = '#3a3a5c';
  ctx.fillRect(cx - 4, cy - 20, 8, 3);
  // Left arm (hoodie sleeve)
  ctx.fillStyle = '#444466';
  ctx.fillRect(cx - 12, cy - 28, 3, 10);
  // Right arm holding shaker
  ctx.fillStyle = '#444466';
  ctx.fillRect(cx + 9, cy - 28, 3, 10);
  // Protein shaker
  ctx.fillStyle = '#ff6b35';
  ctx.fillRect(cx + 10, cy - 22, 4, 7);
  ctx.fillStyle = '#222222';
  ctx.fillRect(cx + 10, cy - 23, 4, 2);
  // Hands
  ctx.fillStyle = '#d4a574';
  ctx.fillRect(cx - 12, cy - 18, 3, 2);
  ctx.fillRect(cx + 9, cy - 18, 3, 2);
  // Hood
  ctx.fillStyle = '#444466';
  ctx.fillRect(cx - 7, cy - 32, 14, 3);
  // Head
  ctx.fillStyle = '#d4a574';
  ctx.fillRect(cx - 5, cy - 39, 10, 7);
  // Headband
  ctx.fillStyle = '#ff6b35';
  ctx.fillRect(cx - 5, cy - 39, 10, 2);
  // Beard
  ctx.fillStyle = '#3d2b1f';
  ctx.fillRect(cx - 4, cy - 34, 8, 2);
  // Eyes (focused)
  ctx.fillStyle = '#111111';
  ctx.fillRect(cx - 3, cy - 37, 2, 1);
  ctx.fillRect(cx + 1, cy - 37, 2, 1);
  // Smirk
  ctx.fillStyle = '#b8845e';
  ctx.fillRect(cx - 1, cy - 34, 3, 1);
  // Chain necklace
  ctx.fillStyle = '#ffd700';
  ctx.fillRect(cx - 3, cy - 30, 6, 1);
  ctx.fillRect(cx - 1, cy - 29, 2, 2);
}
