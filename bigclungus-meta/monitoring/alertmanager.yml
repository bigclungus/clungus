global:
  resolve_timeout: 5m

route:
  receiver: inject
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h

receivers:
  - name: inject
    webhook_configs:
      - url: 'http://127.0.0.1:9095/alert'
        send_resolved: true
