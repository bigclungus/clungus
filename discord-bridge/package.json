{
  "name": "claude-channel-discord-bridge",
  "version": "0.0.1",
  "license": "Apache-2.0",
  "type": "module",
  "bin": "./mcp-bridge.ts",
  "scripts": {
    "start": "bun install --no-summary && bun mcp-bridge.ts",
    "bot": "bun install --no-summary && bun bot.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "discord.js": "^14.14.0"
  }
}
