name: 'Release'
on:
  push:
    tags:
      - 'v*'
jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        platform: [macos-latest, windows-latest]
    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable
          targets: aarch64-apple-darwin, x86_64-apple-darwin, x86_64-pc-windows-msvc
      - name: Install Bun
        uses: oven-sh/setup-bun@v1
      - name: Install deps
        run: bun --cwd clungcord-desktop install
      - name: Install Tauri CLI
        run: bun add -D @tauri-apps/cli@next
        working-directory: clungcord-desktop
      - name: Build app
        uses: tauri-apps/tauri-action@v0.5.25
        with:
          projectPath: clungcord/clungcord-desktop/src-tauri
          distPath: clungcord/clungcord-desktop/src
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.platform }}-clungcord
          path: |
            clungcord-desktop/src-tauri/target/release/bundle/**/*.app
            clungcord-desktop/src-tauri/target/release/bundle/**/*.zip
            clungcord-desktop/src-tauri/target/release/bundle/nsis/*.exe
